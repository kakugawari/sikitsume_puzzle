# このリポジトリと、アプリの作り方

しきつめパズル(正方形のわくにピースを敷き詰めるパズル)。
前半はこのリポジトリの手引き、後半は**次のアプリでもなぞれる作り方**。

> 次のアプリを作るときは、後半の「作り方」と「踏んだ落とし穴」を
> そのまま新しいリポジトリの `CLAUDE.md` にコピーして始める。
> Claude Code はリポジトリ直下の `CLAUDE.md` を毎回読むので、
> これを置いておけば、いちいち説明しなくても同じ進め方になる。

---

## このリポジトリ

### 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # 問題生成のテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
npm run icons      # アイコンを作り直す (絵を変えたときだけ)
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

### 構成

```
index.html / styles.css   画面
src/puzzle.js             問題生成・ポリオミノの回転/反転。ブラウザと node の両方で動く
src/game.js               操作・描画・保存
test/puzzle.test.js       生成のテスト (node --test)
test/browser.js           ブラウザで動かすテスト
scripts/serve.js          開発用サーバー
scripts/make-icons.js     アイコン生成
sw.js, manifest.webmanifest, icon*   PWA まわり
```

### 決めごと

- **ビルドしない。** 素の HTML / CSS / JS。`index.html` を開けば動く
- **ロジックと画面を分ける。** `src/puzzle.js` は DOM を触らない。だから node でテストできる
- **問題は仕組みで正しさを保証する。** 正方形をランダムに分割して作るので、
  生成した時点で解答が存在する。ヒントはその解答を出しているだけ
- 同じ seed からは同じ問題ができる。途中経過は「seed + 各ピースの向きと位置」で保存する
- 日本語でコメントを書く。UI の文言も日本語
- `window.__sikitsume` はテストから盤面をのぞくための入口。`test/browser.js` が使う

### 公開

GitHub Pages の **Deploy from a branch** で配信している。push すれば自動で反映される
(GitHub Actions 方式のデプロイは併用できないので置かないこと)。

---

## 作り方(次のアプリでも同じようにやる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
- **ロジックを画面から切り離す。** テストできる形にしておくと、あとが全部楽になる
- **ビルドを挟まない。** 動かすまでの手数が少ないほど、確かめる回数が増える

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  画面の不具合はユニットテストをすり抜ける
- **体感の問題は計測する。** 「ラグがある気がする」に対しては、
  1 フレームずつ位置を記録して数字を出す。今回それで
  「置いた直後 180ms のあいだ 200px ずれている」を特定した
- **端やずらした条件でも試す。** 最初の配置テストは盤面の原点(0,0)に置いていたので、
  ずれても差が出ず、不具合を見逃していた
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** CPU を 4 倍遅くして計測すると差が見える

### 3. テストプレイしてもらう

1. 全ファイルを 1 枚の HTML にまとめる(CSS も JS も埋め込む)
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**
- マージやデプロイの前に、**実際の設定を確認してから話す。**
  今回 main が存在しないのに「マージすれば反映される」と説明してしまった。
  ブランチ一覧と CI の実行履歴を見れば分かることだった

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う
- 間違えたら、言い訳せずその場で訂正して先へ進む

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 置いた直後にピースが一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めのインライン `transform: translate` を上書きしていた | 位置は `left`/`top` で決める |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| まわすと指の下からピースが逃げる | 回転で縦横が入れ替わり、並び全体がずれていた | 入れ物を正方形にして、回しても大きさを変えない |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | つながっていれば最新を取りにいく |
| ヒントが途中から出せなくなる | 手もとのピースしか探していなかった。必要な形が盤上のよそにあると詰む | 置いてあるものも回収の対象にする |
| 操作のたびに一瞬止まる | 操作ごとに `localStorage` へ同期で書き込み、全体を描き直していた | 書き込みはまとめる。描き直しは変わったぶんだけ |
| 盤面をタップしても反応しない | 上に重ねた層が全面をふさいでいた | 反応させたい所だけ `pointer-events` を有効にする |
